# `stats`

Pure descriptive statistics, paired practical comparison, and outlier views.

See `doc/<locale>/stats/{api,tutorial,design}.md`. The current interval is an
interquartile delta range, not a bootstrap confidence interval.

