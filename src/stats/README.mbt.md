# `stats`

Pure descriptive statistics, paired practical comparison, deterministic
percentile bootstrap intervals, and outlier views.

See `doc/<locale>/stats/{api,tutorial,design}.md`. `compare_paired` retains its
interquartile delta interval; `compare_paired_with_bootstrap` is the explicit
seeded confidence-interval path.
