# `report`

Pure JSONL-to-Plot-IR conversion and safe JSON, accessible SVG, and responsive
self-contained HTML rendering.

See `doc/<locale>/report/{api,tutorial,design}.md`. File IO belongs to `cli`.

